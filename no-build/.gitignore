# Dependencies (the app loads libraries from a CDN — node_modules is not needed at runtime)
node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Local environment files
.env
.env.local

# Firebase tooling cache (if you use the CLI)
.firebase/
firebase-debug.log
